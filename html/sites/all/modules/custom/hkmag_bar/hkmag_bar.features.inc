<?php
/**
 * @file
 * hkmag_bar.features.inc
 */

/**
 * Implements hook_ctools_plugin_api().
 */
function hkmag_bar_ctools_plugin_api($module = NULL, $api = NULL) {
  if ($module == "strongarm" && $api == "strongarm") {
    return array("version" => "1");
  }
}

/**
 * Implements hook_node_info().
 */
function hkmag_bar_node_info() {
  $items = array(
    'bar' => array(
      'name' => t('Bar'),
      'base' => 'node_content',
      'description' => t('Info about a bar, including bar review.'),
      'has_title' => '1',
      'title_label' => t('Title'),
      'help' => '',
    ),
  );
  drupal_alter('node_info', $items);
  return $items;
}
